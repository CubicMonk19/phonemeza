# {$DOMAIN} is substituted from the DOMAIN environment variable (set in
# docker-compose.yml). Caddy provisions and renews a Let's Encrypt certificate
# for this domain automatically, then reverse-proxies all traffic to the app
# container on its internal port 8000.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:8000
}
